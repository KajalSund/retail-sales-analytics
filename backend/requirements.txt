# =============================================================================
# Retail Sales Analytics — Backend Dependencies
# =============================================================================
# Pin minor versions for reproducibility; patch versions float for security.
# =============================================================================

# Web framework
fastapi==0.111.*
uvicorn[standard]==0.29.*       # ASGI server with hot-reload support

# Database
sqlalchemy==2.0.*               # async ORM
asyncpg==0.29.*                 # async PostgreSQL driver
psycopg2-binary==2.9.*          # sync driver used by Alembic migrations

# Configuration
pydantic==2.7.*
pydantic-settings==2.2.*        # loads .env into Settings class

# Migrations (run separately: alembic upgrade head)
alembic==1.13.*

# Dev / testing only
pytest==8.2.*
pytest-asyncio==0.23.*
httpx==0.27.*                   # async test client for FastAPI
